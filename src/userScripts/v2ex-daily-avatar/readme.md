# V2EX Daily Avatar 油猴脚本

本脚本可实现 V2EX 头像每日自动更换，支持自定义头像来源、API、心情表等多种模式。

## ✨ 功能特性
- 支持定时自动更换 V2EX 头像
- 支持自定义 API 头像源，支持变量参数
- 支持一周心情表自定义头像
- 支持通知提醒
- 支持本地配置持久化
- UI 采用 [@wuxh/daily-avatar](https://github.com/wxh16144/daily-avatar) toolkit，界面美观，易于集成

## 🚀 快速使用

1. 安装 [Tampermonkey](https://www.tampermonkey.net/) 或 [Violentmonkey](https://violentmonkey.github.io/)
2. [点击此处安装脚本](https://unpkg.com/@wuxh/daily-avatar/userscripts/v2ex-daily-avatar.user.js)
3. 打开 V2EX，右下角会出现头像自动更换面板

## 🛠️ 配置说明
- **头像来源**：支持随机头像、自定义 API、一周心情表
- **自定义 API**：可配置 API 地址和 Query 参数，支持变量（如 `$DATE`, `$TIMESTAMP`, `$VERSION`, `$RANDOM`）
- **心情表**：可为每周每一天上传不同头像
- **通知**：可配置成功/失败/开始时是否通知

### API 变量说明
| 变量         | 说明               |
| ------------ | ------------------ |
| `$TIMESTAMP` | 当前时间戳         |
| `$DATE`      | 当前日期（YYYY-MM-DD）|
| `$VERSION`   | 当前脚本版本       |
| `$RANDOM`    | 随机数，每次请求变化 |

> 变量必须以 `$` 开头，支持在 API Query 参数 value 中使用。

## 🧩 UI 组件库
本项目 UI 由 [@wuxh/daily-avatar](https://github.com/wxh16144/daily-avatar) 提供，支持独立集成到任意 UserScript 或网页项目。

## 📄 License
MIT
